services:
  app:
    build: .
    container_name: traders-api-app
    volumes:
      - .:/var/www/html
      - ./storage:/var/www/html/storage
    environment:
      APP_ENV: development
    ports:
      - "8080:8080"
